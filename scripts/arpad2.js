/**
 * scripts/app.js
 *
 * This is a sample CommonJS module.
 * Take a look at http://browserify.org/ for more info
 */

'use strict';

function Elo() {
	newRatingIfWon(alice, bob)
  console.log('app initialized');
}

module.exports = Elo;

Elo.prototype.new_alice = function () {
	
console.log("Alice's new rating if she won:", new_alice); // 1605
};
Elo.prototype.new_bob = function () {
	
console.log("Bob's new rating if he won:", new_bob); // 1327
};
function newRatingIfWon(alice, bob) {
	if alice > bob{
		
	console.log("Alice's new rating if she won:", new_alice); }// 1605}
};
//module.exports = newRatingIfWon(alice, bob);